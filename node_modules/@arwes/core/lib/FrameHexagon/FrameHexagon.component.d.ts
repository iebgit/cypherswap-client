import { ReactElement } from 'react';
import { WithAnimatorInputProps } from '@arwes/animation';
import { FrameProps } from '../utils/Frame';
interface FrameHexagonProps<E> extends FrameProps<E> {
    lineWidth?: number;
    squareSize?: number;
    inverted?: boolean;
}
declare function FrameHexagon<E>(props: FrameHexagonProps<E> & WithAnimatorInputProps): ReactElement;
declare namespace FrameHexagon {
    var defaultProps: {
        lineWidth: number;
        squareSize: number;
    };
}
export { FrameHexagonProps, FrameHexagon };

import { ReactElement } from 'react';
import PropTypes from 'prop-types';
import { WithAnimatorInputProps } from '@arwes/animation';
import { FrameProps } from '../utils/Frame';
declare type FRAME_BOX_ORIGIN = 'left' | 'right' | 'top' | 'bottom' | 'center';
interface FrameBoxProps<E> extends FrameProps<E> {
    origins?: FRAME_BOX_ORIGIN[];
    linesWidths?: number[];
}
declare function FrameBox<E>(props: FrameBoxProps<E> & WithAnimatorInputProps): ReactElement;
declare namespace FrameBox {
    var propTypes: {
        origins: PropTypes.Requireable<string | (string | null | undefined)[]>;
        linesWidths: PropTypes.Requireable<number | (number | null | undefined)[]>;
    };
    var defaultProps: {
        origins: string;
        linesWidths: number;
    };
}
export { FrameBoxProps, FrameBox };

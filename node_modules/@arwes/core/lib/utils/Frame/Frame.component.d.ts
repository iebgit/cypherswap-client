import { MutableRefObject, ReactElement, ReactNode } from 'react';
import { CSSObject } from '@emotion/react';
import PropTypes from 'prop-types';
import { NoInfer } from '../types';
import { AnimatedSettings } from '../Animated';
declare type FRAME_DIMENSION = number | string;
declare type FRAME_POINT = FRAME_DIMENSION[];
declare type FRAME_POLYLINE = FRAME_POINT[];
interface FRAME_POLYLINE_CUSTOM {
    polyline: FRAME_POLYLINE;
    lineWidth?: number;
    animated?: AnimatedSettings;
    css?: CSSObject;
}
declare type FRAME_POLYLINE_GENERIC = FRAME_POLYLINE | FRAME_POLYLINE_CUSTOM;
interface FRAME_EFFECTS {
    highlight: () => void;
}
interface FrameProps<E> {
    as?: keyof HTMLElementTagNameMap | keyof SVGElementTagNameMap;
    shapes?: FRAME_POLYLINE[];
    polylines?: FRAME_POLYLINE_GENERIC[];
    lineWidth?: number;
    palette?: string;
    hideShapes?: boolean;
    hidePolylines?: boolean;
    hover?: boolean;
    disabled?: boolean;
    className?: string;
    rootRef?: MutableRefObject<E | null> | ((node: E) => void);
    effectsRef?: MutableRefObject<FRAME_EFFECTS | null> | ((effects: FRAME_EFFECTS) => void);
    children?: ReactNode;
}
declare function Frame<E, P>(props: FrameProps<E> & NoInfer<P>): ReactElement;
declare namespace Frame {
    var propTypes: {
        as: PropTypes.Validator<string>;
        shapes: PropTypes.Requireable<any[]>;
        polylines: PropTypes.Requireable<any[]>;
        lineWidth: PropTypes.Requireable<number>;
        palette: PropTypes.Requireable<string>;
        hideShapes: PropTypes.Requireable<boolean>;
        hidePolylines: PropTypes.Requireable<boolean>;
        hover: PropTypes.Requireable<boolean>;
        disabled: PropTypes.Requireable<boolean>;
        rootRef: PropTypes.Requireable<any>;
    };
    var defaultProps: {
        as: string;
        shapes: never[];
        polylines: never[];
        lineWidth: number;
        palette: string;
    };
}
export { FRAME_DIMENSION, FRAME_POINT, FRAME_POLYLINE, FRAME_POLYLINE_CUSTOM, FRAME_POLYLINE_GENERIC, FRAME_EFFECTS, FrameProps, Frame };

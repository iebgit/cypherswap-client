import { ReactElement } from 'react';
import { WithAnimatorInputProps } from '@arwes/animation';
import { FrameProps } from '../utils/Frame';
interface FramePentagonProps<E> extends FrameProps<E> {
    lineWidth?: number;
    squareSize?: number;
    inverted?: boolean;
}
declare function FramePentagon<E>(props: FramePentagonProps<E> & WithAnimatorInputProps): ReactElement;
declare namespace FramePentagon {
    var defaultProps: {
        lineWidth: number;
        squareSize: number;
    };
}
export { FramePentagonProps, FramePentagon };

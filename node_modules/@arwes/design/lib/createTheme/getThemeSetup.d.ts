import { ThemeSettings, ThemeSetup, Theme } from '../constants';
declare const getThemeSetup: (providedSettings?: ThemeSettings | undefined, extendTheme?: Theme | undefined) => ThemeSetup;
export { getThemeSetup };

import { FC } from 'react';
import { AnimatorSettings } from '../constants';
interface AnimatorProps {
    animator?: AnimatorSettings;
}
declare const Animator: FC<AnimatorProps>;
export { AnimatorProps, Animator };
